fcd() {
  local dir
  dir=$(find . -type d \( -name .git -prune -o -name node_modules -prune \) -o -type d -print 2>/dev/null | fzf +m) && cd "$dir"
}

php7() {
    docker run -it --rm \
        -v "$(pwd):/workspace" \
        -w /workspace \
        php:7.4-cli \
        bash
}
